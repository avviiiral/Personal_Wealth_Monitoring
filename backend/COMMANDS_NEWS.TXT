# Create Task 
schtasks /create /tn "PWMS Portfolio News Monitor" /tr "D:\PWMS\Personal_Wealth_Monitoring\backend\run_news_monitor.bat" /sc minute /mo 45 /st 00:00 /ru "itadmin" /rp *

# Confirm it registered
schtasks /query /tn "PWMS Portfolio News Monitor" /v /fo LIST

# Trigger it manually right now, to confirm it actually works under the Task Scheduler context (not just your interactive shell)
schtasks /run /tn "PWMS Portfolio News Monitor"

# Remove it later if you ever need to
schtasks /delete /tn "PWMS Portfolio News Monitor" /f